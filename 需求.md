1.分为2层，一是中间的点心层，二是外侧的聚焦层。

2.点心层可以提供多种内置样式例如圆、方、三角。

3.聚焦层提供多种内置样式例如>、<、）、V、-等。

4.点心层设置：

- 大小的设置，支持1.输入设置大小 2.滑块控制大小。
- 自转设置，支持1.输出设置自转幅度 2.滑块控制自传幅度
- 透明度设置，支持1.输出设置透明度 2.滑块控制透明度
- 内置样式可以自己调控点心层颜色，支持颜色选择或者rgb输入

5.聚焦层设置：

- 大小的设置，支持1.输入设置大小 2.滑块控制大小。
- 自转设置，支持1.输出设置自转幅度 2.滑块控制自传幅度
- 透明度设置，支持1.输出设置透明度 2.滑块控制透明度
- 内置样式可以自己调控聚焦层颜色，支持颜色选择或者rgb输入
- 距离设置，支持1.输入设置聚焦层距离点心的距离 2.滑块控制聚焦层距离点心的距离 3.距离可为负数。
- 聚焦层是分为四块来围绕点心，可以自由选择决定每个聚焦层块的隐藏。

6.支持自定义输入点心、聚焦的图案，可以是图片格式。

7.主界面的状态栏提供

- 锁定/解锁准星挪动。
- 关闭项目
- 点击自动挪到屏幕中心。
- 挪动边框自定义控制准星位置。
- 点击进入层的设置界面。
- 层的设置界面提供改快捷键，以打开本项目主界面状态栏
- 主界面只有边框和设置有颜色为黑便于控制准星边界。以及内部准星预览来实时观测调控样式。
- 层的设置界面是从框向右展开，内部用卡片分开点心层和聚焦层，同时提供改快捷键来调控打开/关闭状态栏(默认F9)